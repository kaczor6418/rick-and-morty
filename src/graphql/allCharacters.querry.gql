query {
    characters(filter: { name: "" }) {
        info {
            count
        }
        results {
            id,
            name,
            gender,
            species,
            episode {
                episode
            }
        }
    }
}
